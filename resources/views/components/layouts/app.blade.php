{{-- 
    Layout Base Principal do Sistema
    
    Este é o layout padrão que o Livewire busca.
    Utiliza o component layout-base para permitir troca dinâmica de templates.
--}}
<x-layout-base>
    {{ $slot }}
</x-layout-base>
